The dictionary of problem definition can be read from the codes,

a=np.load('n=10.py')
dict=a.item()

The keys  of the dictionary represent the number of the clauses while the values represent the clauses of different realizations.

